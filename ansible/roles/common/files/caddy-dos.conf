# Fail2Ban filter for Caddy DoS/DDoS attacks
# Detects potential denial of service attacks through rapid requests (JSON logs)

[Definition]

# Match any request by remote_ip (triggered by frequency via jail maxretry)
failregex = .*"remote_ip":"<HOST>".*

ignoreregex =
