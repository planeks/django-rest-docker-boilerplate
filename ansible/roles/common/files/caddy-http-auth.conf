# Fail2Ban filter for Caddy HTTP authentication failures
# Matches 401 Unauthorized and 403 Forbidden responses in JSON logs

[Definition]

# Match 401/403 status codes in Caddy JSON logs
failregex = .*"remote_ip":"<HOST>".*"status":\s*(401|403).*

ignoreregex =
